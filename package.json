{
  "name": "fiiocontrol-oss",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/write-trust-hash.mjs $(git rev-parse HEAD)",
    "preview": "pnpm run build && wrangler dev",
    "deploy": "pnpm run build && wrangler deploy"
  },
  "dependencies": {
    "solid-js": "^1.9.12"
  },
  "devDependencies": {
    "vite": "^8.0.4",
    "vite-plugin-solid": "^2.11.12",
    "wrangler": "^4.92.0"
  },
  "packageManager": "pnpm@11.1.2"
}
